<%= simple_form_for [@furniture, @booking] do |f| %>
  <%= f.input :renting_start, as: :date, label: "Start Date" %>
  <%= f.input :renting_end, as: :date, label: "End Date" %>
  <%= f.input :comment, as: :text, label: "Comment" %>
  <%= f.button :submit %>
<% end %>
#comments 2
